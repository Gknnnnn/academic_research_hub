# ANN FeedForward

Method family:

- machine-learning

Plain language:

- girdileri gizli katmanlardan geçirerek doğrusal olmayan çıktı tahmini üretir

Equation:

```latex
\hat{y} = f\left(\sum_{h=1}^{H} w_h \, g\left(\sum_{j=1}^{J} v_{hj} x_j + b_h\right) + c\right)
```

Wolfram Alpha query:

- simplify nested activation function expression

Linked method map:

- `100-Literature/150-Method-Maps/ANN.md`
